numpy
tqdm
# PyTorch 2.10's Windows wheel exposes Gloo but cannot construct a usable
# transport ("unsupported gloo device").  The Linux/Modal control stays pinned
# to 2.10; local Windows smoke tests use the first verified fixed wheel.
torch==2.13.0; sys_platform == "win32"
torch==2.10.0; sys_platform != "win32"
huggingface-hub
kernels
setuptools
typing-extensions==4.15.0
datasets
requests
tiktoken
