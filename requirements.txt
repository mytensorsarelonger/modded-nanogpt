numpy
tqdm
# PyTorch 2.10 is not a valid control runtime: its compiled graph produced
# non-finite weights on the second update. 2.13 is verified on Linux/CUDA and
# also fixes the unusable Windows Gloo transport.
torch==2.13.0
huggingface-hub
kernels
setuptools
typing-extensions==4.15.0
datasets
requests
tiktoken
